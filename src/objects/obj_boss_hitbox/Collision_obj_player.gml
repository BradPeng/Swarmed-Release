/// @description Insert description here
// You can write your code in this editor
if (array_contains_value(damage_frames, floor(image_index)+1)) {
	hurt_player(source_direction, source_force, source_damage);
}