/// @description Delete mp_grid!
/*if (variable_global_exists("mp_grid") && global.mp_grid != noone) {
    mp_grid_destroy(global.mp_grid); // Destroy the grid to free memory
    global.mp_grid = noone; // Optional: reset the variable to noone
}*/
