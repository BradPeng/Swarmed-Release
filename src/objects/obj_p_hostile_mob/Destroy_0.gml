/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();
global.level_score += score_on_death
if (path_exists(path)) {
	path_delete(path);
} 
